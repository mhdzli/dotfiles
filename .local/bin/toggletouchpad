#!/bin/bash

# TOUCHPAD_ID="$(swaymsg -t get_inputs | jq -r '.[] | select(.type == "touchpad") | .identifier')"
# STATE="$(swaymsg -t get_inputs | jq -r '.[] | select(.type == "touchpad") | .libinput.send_events')"
swaymsg input "type:touchpad" events toggle


